var DataSource = require('../index').DataSource;
var db = new DataSource('memory');

var Post = db.define('Post', {name: String});
Post.hasMany('notes', { model: 'Note' });

// note.js(on)
var Note = db.define('Note', {name: String});
Note.belongsTo('post', { model: 'Post' });
